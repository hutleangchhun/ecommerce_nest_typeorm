# VPS Production Environment Variables - Local Services
# Copy this file to .env on your VPS and update the values

# Database Configuration (Local PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_secure_database_password_here
DB_NAME=ecommerce_db

# Redis Configuration (Local Redis)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password_if_needed

# Application Security
JWT_SECRET=your_super_secure_jwt_secret_key_for_production
JWT_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# Note: NODE_ENV=production is set in docker-compose.vps.yml
# Note: Ensure PostgreSQL and Redis are installed and running on your VPS